#Comando para inicializar o migrate
#flask db init

#Comando para atualizar o migrate
#flask migrate -m "mensagem"
#flask db migrate
#flask db upgrade

#Ativar a venv
#source venv/bin/activate

#Desativar a venv
#deactivate

#Instalar as dependências
# pip install -r requirements.txt

#Como importar o arquivo vacinas.cvs?
#No PGAdmin clique na database e selecione a tabela vacina;
#com o botão direito selecione Import/Export e selecione o arquivo;
#no mesmo modal da importação selecione Columns e só deixe a coluna nome.
#Espere que funcione, bye chingu!